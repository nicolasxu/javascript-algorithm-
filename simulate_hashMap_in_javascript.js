/**
 * Build a hash table to store data
 * HashMap(Key, Value)
 * 
 * - A hash function to map key to value
 * - Store value object in linked list at index hash(key)
 * - implement hash table with binary search tree. 
 *   keep the tree balanced so that the lookup time 
 *   is O(LogN)
 *
 *
 *
 * 
 */

